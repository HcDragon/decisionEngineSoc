# Compatible with Mac ARM (Apple Silicon) and Windows (CPU-only, no CUDA)
fastapi>=0.111.0
uvicorn>=0.29.0
streamlit>=1.35.0
requests>=2.31.0
pandas>=2.2.0
plotly>=5.22.0
xgboost>=2.0.3          # native ARM64 wheels available via pip; no CUDA required
scikit-learn>=1.4.2
joblib>=1.4.0
pyyaml>=6.0.1
